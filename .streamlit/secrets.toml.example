# Copy this to secrets.toml (locally) or add in Streamlit Cloud settings.
# These enable persistent portfolio storage via GitHub API.
# Without these, portfolio data is stored locally (fine for Mac, ephemeral on cloud).

GITHUB_TOKEN = "ghp_your_personal_access_token_here"
GITHUB_REPO = "singularity99/Gold-Silver-Tracker"

# Groq API key for AI chat (free at https://console.groq.com)
GROQ_API_KEY = "gsk_your_groq_api_key_here"
